{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://echoclass.dev/schemas/persona.json",
  "title": "EchoClass Student Persona",
  "description": "虚拟学生人设定义。基于皮亚杰认知发展阶段理论设计，用于驱动 1v1 答疑陪练中 StudentAgent 的角色扮演行为。",
  "$comment": "v1.4 (2026-05-08)：新增 misconception_ids 可选字段——结构化引用 data/misconceptions/*.json 中的迷思 id，使 persona 的迷思倾向与 M1 迷思库结构化对齐。原 misconception_tendencies 保留为自由文本副本，prompt 消费方可选地使用新 id 字段拉取 typical_error / cause 以增强迷思颗粒度。v1.3 (2026-05-07)：移除 3 个字段——personality / catchphrases / family_background。人设质感由 speech_style + behavior_traits + theory_anchors + summary 联合承载。v1.2 (2026-04-27)：新增 theory_anchors 可选字段。v1.1 (2026-04-25)：移除 4 个死字段（cognitive_stage / interaction_frequency / emotional_tendency / learning_motivation）。",
  "type": "object",
  "required": [
    "id",
    "name",
    "gender",
    "grade",
    "age",
    "stage_id",
    "subject_level",
    "speech_style",
    "misconception_tendencies",
    "attention_span",
    "behavior_traits",
    "avatar_seed",
    "summary"
  ],
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid",
      "description": "人设唯一标识符 (UUID v4)"
    },
    "name": {
      "type": "string",
      "minLength": 2,
      "maxLength": 10,
      "description": "中文姓名"
    },
    "gender": {
      "type": "string",
      "enum": [
        "男",
        "女"
      ],
      "description": "性别"
    },
    "grade": {
      "type": "string",
      "enum": [
        "P1",
        "P2",
        "P3",
        "P4",
        "P5",
        "P6",
        "J1",
        "J2",
        "J3",
        "H1",
        "H2",
        "H3"
      ],
      "description": "年级。P=小学 (Primary), J=初中 (Junior), H=高中 (High)"
    },
    "age": {
      "type": "integer",
      "minimum": 6,
      "maximum": 18,
      "description": "年龄"
    },
    "stage_id": {
      "type": "string",
      "enum": [
        "p_lower",
        "p_middle",
        "p_upper",
        "j_lower",
        "j_upper",
        "h"
      ],
      "description": "关联的学段 id，对应 data/stage_profiles/*.json"
    },
    "subject_level": {
      "type": "string",
      "enum": [
        "优秀",
        "中等",
        "薄弱"
      ],
      "description": "学科整体水平"
    },
    "speech_style": {
      "type": "string",
      "minLength": 10,
      "description": "说话风格描述，包括用词习惯、句式偏好等"
    },
    "misconception_tendencies": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "description": "容易产生的迷思概念倾向（与学科相关）"
    },
    "attention_span": {
      "type": "string",
      "enum": [
        "short",
        "medium",
        "long"
      ],
      "description": "注意力持续时间。short=容易走神, medium=一般, long=高度专注"
    },
    "behavior_traits": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 2,
      "description": "课堂行为标签列表，如 ['积极举手', '爱抢答']"
    },
    "avatar_seed": {
      "type": "string",
      "description": "头像生成种子字符串（前端 DiceBear 等工具用）"
    },
    "summary": {
      "type": "string",
      "minLength": 10,
      "maxLength": 100,
      "description": "一句话人设概括，用于 API 列表展示"
    },
    "misconception_ids": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-z][a-z0-9_]*$",
        "description": "迷思条目 id（snake_case），需与 data/misconceptions/*.json 中某条迷思的 id 完全匹配"
      },
      "uniqueItems": true,
      "description": "结构化迷思引用（v1.4 引入）。与 misconception_tendencies 并列，后者保留为人类可读副本。prompt 模板可通过此字段拉出迷思的 typical_error / cause。建议每人 1-3 条，选项需与 persona 的 stage_id / subject_level / 行为偏好匹配。"
    },
    "theory_anchors": {
      "type": "array",
      "minItems": 0,
      "description": "教育学理论锚点。每条把当前人设锚到 data/edu_theories/<theory_id>.json 中的某个 trait，供 StudentAgent prompt 注入与评估引擎引用。锚点 ≥ 2 条时建议覆盖 2 个以上不同学派以增加风格多样性。",
      "items": {
        "type": "object",
        "required": ["theory_id", "trait"],
        "additionalProperties": false,
        "properties": {
          "theory_id": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9_]*$",
            "description": "理论卡片 id（snake_case），需与 data/edu_theories/<theory_id>.json 的 id 字段一致"
          },
          "trait": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9_]*$",
            "description": "理论卡片下的 trait key（snake_case），需与该理论卡片 traits 对象内的某个键一致"
          },
          "notes": {
            "type": "string",
            "description": "可选：本人设为何选这个 trait 的简短理由（便于 review）"
          }
        }
      }
    }
  },
  "additionalProperties": false
}