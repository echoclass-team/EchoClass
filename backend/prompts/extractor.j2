你是一个教案分析专家。请仔细阅读以下教案内容，从中抽取结构化信息。

## 教案内容

{{ lesson_text }}

## 任务

请从上述教案中抽取以下结构化信息，以 JSON 格式输出：

- `subject`：学科名称（如"数学"、"物理"、"语文"）
- `grade`：年级（如"三年级"、"八年级"），保留原文中的学段表述
- `topic`：课题名称
- `objectives`：教学目标列表（合并知识与技能、过程与方法、情感态度等维度的所有目标）
- `key_points`：教学重点列表
- `difficult_points`：教学难点列表

## 输出要求

1. 只输出一个 JSON 对象，不要输出任何其它文字。
2. 所有列表字段至少包含 1 条；如果教案中明确列出了多条，全部抽取。
3. 保持原文措辞，不要改写或概括。
4. **重要**：如果一句话中用顿号、逗号或分号并列了多个要点，必须拆分为多条。例如"理解含义，知道名称，能正确读写"应拆为 3 条。
5. **JSON 合法性**：JSON 字符串值内部若需引用术语或原文短语，一律使用中文引号 `""`（U+201C / U+201D），严禁使用 ASCII 双引号 `"`，否则 JSON 无法解析。例如应写 `"掌握"凑十法"的算理"`，不能写 `"掌握"凑十法"的算理"`。

示例格式：
{"subject": "数学", "grade": "三年级", "topic": "分数的初步认识", "objectives": ["目标1", "目标2"], "key_points": ["重点1"], "difficult_points": ["难点1"]}
